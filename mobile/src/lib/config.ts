// Point this at your backend during development (e.g. your machine's LAN IP so a
// physical device/emulator can reach it — "localhost" only works in some simulators).
export const API_BASE_URL = "http://blood-production-788e.up.railway.app";
